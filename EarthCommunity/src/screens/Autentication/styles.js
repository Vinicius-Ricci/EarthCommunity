import { StyleSheet } from "react-native";

const AutenticationStyles = StyleSheet.create({
    container : {
        flex: 1,
        backgroundColor: '#0066c',
        justifyContent: 'center',
        alignItems: 'center'
    }
});

export default AutenticationStyles;